const logger = require("../../config/logger")


const placeOrder = async (req,res)=>{
    logger.info("placeOrder Route hit...");
   
    try {
        
    } catch (error) {
        
    }
}

module.exports = {
    placeOrder
}